# Top-level build file — targets live in their own packages.

alias(
    name = "nomuc",
    actual = "//src/nomu-cli:nomuc",
)

alias(
    name = "install",
    actual = "//tools/install:install",
)
