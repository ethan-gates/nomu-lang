

fun fib(n: Int) -> Int {
    if n <= 1 {
        return n
    }

    return fib(n: n - 1) + fib(n: n - 2)
}

fun main() {
    let r = fib(n: 100)
    print(r)              // 9227465 — observed so the benchmark isn't dead-code-eliminated under -O
}
